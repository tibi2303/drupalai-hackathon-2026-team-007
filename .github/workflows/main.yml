# Github
name: issue-greeter
on:
  push:
    branches:
      - main
jobs:
  notify-devpanel-main:
    runs-on: ubuntu-latest
    steps:
      - name: notify
        run: |
          curl -s -X PUT https://console.devpanel.com/webhooks/applications/6978cda1c8af5ad7b33f0ef7/git -d "{\"username\": \"${{ secrets.DP_USERNAME }}\", \"token\": \"${{ secrets.DP_ACCESS_TOKEN }}\"}" -H "content-type: application/json"
