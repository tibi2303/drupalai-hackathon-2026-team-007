{{
  include(
    '@oe-bcl/bcl-gallery/gallery-item.html.twig',
    {
      item: item|default({}),
      carousel_id: carousel_id|default(''),
      modal_id: modal_id|default(''),
      carousel_anchor: carousel_anchor|default(''),
      icon_path: icon_path|default(''),
    },
    with_context: false
  )
}}
