{{
  include(
    '@oe-bcl/bcl-banner/banner.html.twig',
    {
      title: title|default(''),
      title_tag: title_tag|default('div'),
      title_link: title_link|default({}),
      description: description|default(''),
      link: link|default({}),
      image: image|default(''),
      shade: shade|default(false),
      hero: hero|default(false),
      full_width: full_width|default(false),
      fixed_height: fixed_height|default(false),
      content_classes: content_classes|default(''),
    },
    with_context: false
  )
}}
