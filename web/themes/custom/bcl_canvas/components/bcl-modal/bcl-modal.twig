{{
  include(
    '@oe-bcl/bcl-modal/modal.html.twig',
    {
      size: size|default(''),
      id: id|default(false),
      fullscreen_responsive: fullscreen_responsive|default(''),
      static_backdrop: static_backdrop|default(false),
      vertically_centered: vertically_centered|default(false),
      verticaly_centered: verticaly_centered|default(false),
      scrollable: scrollable|default(false),
      header: header|default(''),
      messages: messages|default(''),
      body: body|default(''),
      footer: footer|default(''),
    },
    with_context: false
  )
}}
