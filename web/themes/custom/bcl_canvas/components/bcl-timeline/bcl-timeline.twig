{{
  include(
    '@oe-bcl/bcl-timeline/timeline.html.twig',
    {
      id: id|default(null),
      title: title|default(''),
      title_tag: title_tag|default('h2'),
      title_link: title_link|default({}),
      toggle_collapsed: toggle_collapsed|default(''),
      toggle_expanded: toggle_expanded|default(''),
      items: items|default([]),
      hide: hide|default({}),
      icon_path: icon_path|default(''),
    },
    with_context: false
  )
}}
