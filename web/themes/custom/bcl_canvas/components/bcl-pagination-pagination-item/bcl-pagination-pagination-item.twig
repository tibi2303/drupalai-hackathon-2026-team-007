{{
  include(
    '@oe-bcl/bcl-pagination/pagination-item.html.twig',
    {
      path: path|default(''),
      label: label|default(''),
      disabled: disabled|default(false),
      active: active|default(false),
      aria_label: aria_label|default(''),
      icon: icon|default({}),
    },
    with_context: false
  )
}}
