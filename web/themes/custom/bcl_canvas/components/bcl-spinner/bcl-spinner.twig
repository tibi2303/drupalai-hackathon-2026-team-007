{{
  include(
    '@oe-bcl/bcl-spinner/spinner.html.twig',
    {
      type: type|default('border'),
      variant: variant|default('primary'),
      size: size|default('sm'),
      assistive_text: assistive_text|default('Loading…'),
    },
    with_context: false
  )
}}
