{{
  include(
    '@oe-bcl/bcl-button/button.html.twig',
    {
      label: label|default(''),
      variant: variant|default('primary'),
      size: size|default('md'),
      type: type|default('button'),
      outline: outline|default(false),
      disabled: disabled|default(false),
      text_nowrap: text_nowrap|default(false),
      assistive_text: assistive_text|default(''),
      id: id|default(''),
      icon: icon|default({}),
      icon_position: icon_position|default(''),
      icon_spacing: icon_spacing|default(''),
      spinner: spinner|default({}),
      show_spinner: show_spinner|default(false),
      clean_class: clean_class|default(false),
    },
    with_context: false
  )
}}
